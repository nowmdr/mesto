import PopupWithForm from "./PopupWithForm";
export default class PopupWithEditAvatarForm extends PopupWithForm {
    constructor({changeData},selector) {
        super({changeData},selector);
    }
}